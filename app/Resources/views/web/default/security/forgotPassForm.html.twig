{% extends 'web/' ~ config.website_skin ~ '.html.twig' %}

{% block title %} {{ parent() }} &bull; {{ lang.SECURITY_FORGOT_PASS }}{% endblock %}

{% block body %}

    <div id="forgot_pass_hero" class="row">
        <div class="col-12 mt-100 mb-100 pt-10">

            <div class="row">
                <div id="outer-box" class="col-md-4 col-sm-6 mx-auto">

                    <div class="row">
                        <div class="col-12">

                            <div class="row no-gutters">
                                <div class="col-2 text-left p-2">
                                    <img id="identification_image"
                                         src="/assets/images/web/{{ config.website_skin }}/logo.png"
                                         alt="Logo {{ config.web_name }}"
                                         data-retina="true" />
                                </div>
                                <div id="identification-title" class="col-10">
                                    {{ lang.SECURITY_FORGOT_PASS }}
                                </div>
                            </div>

                        </div>
                        <div class="col-12">
                            <hr>
                        </div>
                        {% if errors|length > 0 %}
                            <div class="col-12" style="margin-bottom: 10px;">
                                <div class="alert alert-danger">
                                    {{ lang.ERRORS_FOUND }}:
                                    <ul>
                                        {% for error in errors %}
                                            <li>{{ error }}</li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            </div>
                            <hr>
                        {% endif %}
                        <div class="col-12">

                            <form role="form" action="/forgot-password" method="post">

                                <div class="form-group">
                                    <label for="email">{{ lang.SECURITY_FORGOT_USERNAME_EMAIL }}</label>
                                    <input type="text"
                                           class="form-control"
                                           id="email"
                                           name="email"
                                           required="required"
                                           autocomplete="email"
                                           placeholder="{{ lang.SECURITY_FORGOT_USERNAME_EMAIL_ENTER }}">
                                </div>

                                <div style="margin: 20px;"></div>

                                <button type="submit" name="btn_submit" class="btn btn-primary btn-lg btn-block">
                                    <i class="{{ sign_in }}" aria-hidden="true"></i> {{ lang.SECURITY_FORGOT_SEND }}
                                </button>

                                <input type="hidden" name="auth_token" value="{{ data.auth_token }}" />

                            </form>
                        </div>
                    </div>

                </div>
            </div>

        </div>
    </div>

{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="/bundles/framework/css/web/{{ config.website_skin }}/security.css" rel="stylesheet">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function(){

            $('#email').focus();

        });
        (function($) {
            'use strict';

        })(jQuery);
    </script>
{% endblock %}
